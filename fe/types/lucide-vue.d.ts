declare module "lucide-vue" {
  import { DefineComponent } from "vue";
  const component: DefineComponent<{}, {}, any>;
  export const Moon: typeof component;
  export const Sun: typeof component;
  export const X: typeof component;
  export const Menu: typeof component;

  // Tambah ikon lain jika dipakai
}
